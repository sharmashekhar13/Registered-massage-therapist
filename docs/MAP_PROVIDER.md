// Admin web: Mapbox example integration note

// The admin web and mobile scaffolds will reference MAP_PROVIDER and MAPBOX_ACCESS_TOKEN from environment variables.
// Mapbox is the default provider for dev, but the UI code is provider-agnostic and can be switched to Google Maps by changing MAP_PROVIDER to 'google' and supplying GOOGLE_MAPS_API_KEY.
